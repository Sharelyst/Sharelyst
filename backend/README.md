# Backend README
This directory contains the backend code for the project. It is built using Node.js with the Express framework and uses SQLite as the database.

## Setup Instructions
- run `npm install` to install all dependencies before starting the server.
- run `npm start` to start the server.

## Future Notes
- Remove verbose logging (form sqlite3) in production mode.